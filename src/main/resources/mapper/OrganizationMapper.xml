<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acmp.compute.mapper.OrganizationMapper">

    <resultMap id="BaseResultMap" type="com.acmp.compute.entity.Organization">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO organization (id, name, created_at, updated_at)
        VALUES (#{id}, #{name}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM organization WHERE id = #{id}
    </select>
</mapper>
